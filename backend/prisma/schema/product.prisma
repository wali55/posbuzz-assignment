model Product {
  id             String     @id @default(uuid())
  name           String
  sku            String     @unique
  price          Decimal    @db.Decimal(10, 2)
  stock_quantity Int        @default(0)
  saleItems      SaleItem[]
}